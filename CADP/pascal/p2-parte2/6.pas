program seis;
    procedure leer(var precio:real ; var code:integer ; var tipo:string);
        begin
            readln(precio);
            readln(code);
            readln(tipo);
        end;

    procedure codeMasBaratos();
        begin

        end;

    procedure pantMasCaro();
        begin

        end;

    procedure promedio();
        begin

        end;
var
    precio:real;
    code:integer;
    tipo:string;
begin
    leer(precio,code,tipo);
end.